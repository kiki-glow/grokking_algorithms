# grokking_algorithms

## 1. Binary Search

with binary search, you guess the middle number and eliminate half the remaining numbers every time.
- for any list *n*, binary search will take *log₂ n* steps to run in the worst case, whreas simple search will take *n* steps